{
  "name": "@dating-app/events",
  "version": "1.0.0",
  "description": "Event-driven architecture library for Dating App microservices",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "clean": "rm -rf dist",
    "postinstall": "npm run build"
  },
  "keywords": [
    "event-driven",
    "microservices",
    "sqs",
    "sns",
    "serverless"
  ],
  "author": "Dating App Team",
  "license": "MIT",
  "peerDependencies": {
    "@nestjs/common": "^10.0.0 || ^11.0.0",
    "@nestjs/config": "^3.0.0 || ^4.0.0",
    "@aws-sdk/client-sqs": "^3.0.0",
    "@aws-sdk/client-sns": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typeorm": "^0.3.0",
    "typescript": "^5.1.0"
  }
}
